@model List<Resturants.Models.Category>
@{
    ViewData["Title"] = "Menu";
}

<h2>Notes</h2>

@foreach (var category in Model)
{
    <h4>Category: @category.ShortDescription</h4>

    <ul>
        @foreach (var meal in category.Meals)
        {
            <li>
                @meal.Name @if (meal.Note != null)
                {<span>@meal.Note.Description</span>}
            </li>
        }
    </ul>
}

